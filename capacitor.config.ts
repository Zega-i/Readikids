import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.readikids.readikids',
  appName: 'ReadiKids',
  webDir: 'dist',
  android: {
    backgroundColor: '#FFF6E9'
  }
};

export default config;
